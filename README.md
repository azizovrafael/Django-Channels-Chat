# Django-Channels-Chat

## Django Stream Api

### Create virtualenv

1) Start Redis Server
* MacOS:
  ** ```brew install redis```
  ** ```redis-server```
2) ```pip3 install -r req.txt```
3) ```python3 manage.py runserver```
  
  
